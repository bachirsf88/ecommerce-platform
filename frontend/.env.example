# Use /api when the frontend talks through the Vite proxy.
# For a direct backend URL, set something like http://127.0.0.1:8000/api instead.
VITE_API_BASE_URL=/api

# Backend origin only. Do not include /api here.
VITE_BACKEND_URL=http://127.0.0.1:8000
